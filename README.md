# AeroPeek

A native, keyboard-first macOS workspace overlay for AeroSpace.

Open `AeroPeek.xcodeproj`, select the AeroPeek scheme, and run.

Controls: Up/Down selects, Home/End jumps, 1–9/A/C/N switches directly, Return switches to the selection, and Escape closes the overlay.
